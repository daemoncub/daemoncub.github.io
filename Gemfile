source "https://rubygems.org"

gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.12"
end

# GitHub Pages will provide the theme; this is for local testing only
platforms :mingw, :x64_mingw, :mswin do
  gem "tzinfo", ">= 1"
  gem "tzinfo-data"
end
